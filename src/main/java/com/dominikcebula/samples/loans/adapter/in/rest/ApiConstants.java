package com.dominikcebula.samples.loans.adapter.in.rest;

import java.net.URI;

interface ApiConstants {
    String API_BASE = "/api/v1/loan-applications";
    URI API_BASE_URI = URI.create(API_BASE);
}
